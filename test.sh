
minimal_manifest=14

if [ "${minimal_manifest}" != "14" ] && [ "${minimal_manifest}" != "14.1" ]; then
    echo "ini minmal manifest lain"
else
#BUILD KETIKA MINIMAL MANIFEST 14 / 14.1
   echo "ini manifest 14"
fi
